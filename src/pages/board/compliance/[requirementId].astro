---
/**
 * Redirect: /board/compliance/[id] → /portal/board/compliance/[id]
 * Board pages have been moved to /portal/board/* for consistency
 */
export const prerender = false;
const { requirementId } = Astro.params;
return Astro.redirect(`/portal/board/compliance/${requirementId}`, 301);
---
