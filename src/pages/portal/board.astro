---
/**
 * BOARD Landing Zone Redirect: /portal/board → /portal/board/dashboard
 *
 * Redirects to the new unified board dashboard.
 * This maintains backward compatibility for old links.
 */
export const prerender = false;
return Astro.redirect('/portal/board/dashboard', 302);
---
