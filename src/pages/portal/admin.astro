---
/**
 * ADMIN Landing Zone Redirect: /portal/admin → /portal/admin/dashboard
 *
 * Redirects to the new unified admin dashboard.
 * This maintains backward compatibility for old links.
 */
export const prerender = false;
return Astro.redirect('/portal/admin/dashboard', 302);
---
