---
/**
 * ARB Landing Zone Redirect: /portal/arb → /portal/arb/dashboard
 *
 * Redirects to the ARB dashboard (which currently redirects to /portal/arb-dashboard).
 * This maintains backward compatibility for old links.
 */
export const prerender = false;
return Astro.redirect('/portal/arb/dashboard', 302);
---
