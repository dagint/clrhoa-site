---
export const prerender = false;

import { SESSION_COOKIE_NAME } from '../../lib/auth';

const response = Astro.redirect('/portal/login');
response.headers.set(
  'Set-Cookie',
  `${SESSION_COOKIE_NAME}=; Path=/; HttpOnly; SameSite=Lax; Max-Age=0`
);
return response;